{
  "name": "orion-dev",
  "scripts": {
    "dev": "vercel dev",
    "build": "npm --prefix server run build && vite build",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@base44/sdk": "^0.8.30",
    "@base44/vite-plugin": "^1.0.20"
  }
}
